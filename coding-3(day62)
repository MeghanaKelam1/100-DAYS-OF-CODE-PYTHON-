#include <stdio.h>
int main(void) {
    //A Simple Hello World
int a;
scanf("%d",&a);
if(a%2 == 0)
printf("Even");
else if(a%2 ==  1)
printf("Odd");
else
printf("Zero");
    return 0;
}

#include <stdio.h>
int main(void) {
    //A Simple Hello World
   int i,n=9,in,no;
   scanf("%d",&in);
   for(i=1;i<=in;i++)
   {
       printf("%d ",n*i);
   }
   if(in == 0)
   {
   printf("NULL");
   }
    return 0;
}

#include <stdio.h>
int main(void) {
    //A Simple Hello World
 int A,B,C;
 scanf("%d\n%d\n%d",&A,&B,&C);
 if(A>B && A>C)
 printf("%d",A);
 else if(B>A && B>C)
 printf("%d",B);
 else
 printf("%d",C);
    return 0;
}

